# Elara 项目上下文

## GitHub 仓库

- 提交地址：https://github.com/keosng/Elara.git

## 项目定位

Elara 是一个剧情向 AI 陪伴游戏，而不是普通的通用 AI 聊天应用。用户会与单一核心角色 Elara 长期互动，通过对话、线索、网页解谜和选择影响信任与好感度，探索多个结局。通关后关系继续存在，用户可以继续陪伴或重新探索其他路线。

核心表达：有终点的故事，无终点的关系。

## 技术栈与入口

- 后端：NestJS 11 + TypeScript
- 数据库：PostgreSQL + Prisma 7
- 前端：原生 HTML/CSS/JavaScript
- 当前主页：`public/index.html`，用于保证基础聊天功能可用
- 未来主页候选：`public/corridor-demo.html`，场景化前端原型，完善后逐步接入后端
- 启动入口：`src/main.ts`
- 聊天模块：`src/chat/`
- Prisma 封装：`src/prisma/`
- 数据模型和迁移：`prisma/`

## 当前已完成

- AI 普通回复和 SSE 流式回复
- 多会话创建、列表、恢复和软删除
- 消息持久化和消息状态：`PENDING`、`COMPLETED`、`FAILED`
- 长对话滚动摘要
- `User` 表和 `Conversation.userId` 数据模型
- `ConfigModule` + Joi 启动配置校验
- `ChatService` 通过 `ConfigService` 读取环境变量

- 用户注册、登录和密码校验
- PostgreSQL Session 存储和 HttpOnly Cookie
- SessionAuthGuard 鉴权
- 会话和消息按当前用户隔离
- Token 用量记录、汇总与查询（支持精确、部分估算和全估算，网页展示用户与各会话累计）

## 当前未完成

- Elara 人格、长期记忆、好感度和信任系统
- 网页解谜、多结局和通关后陪伴模式
- Token 成本汇总与预算控制
- 全流程统一跟踪日志

用户注册、登录、Session 鉴权、前端注册、登录态展示和注销入口的基础链路已经完成；后续仍需补充 CSRF 防护。

## 方法调用关系注释规则

涉及业务流程的类和方法必须使用中文注释说明调用关系，不能只写“处理数据”这类泛泛描述。每个公开方法至少说明：

- 谁或什么事件触发它（前端操作、HTTP 路由、定时器、NestJS 生命周期或上层方法）
- 输入参数的来源、含义和关键约束
- 它按什么顺序调用哪些方法、数据库操作或外部服务
- 成功时返回什么，返回值会被谁继续使用
- 失败时抛出什么、由哪一层处理，以及是否需要回滚或更新失败状态

私有方法也要说明它在完整调用链中的位置。例如：`Controller 方法 → Service 对外方法 → Service 私有协调方法 → Prisma/AI → 返回 Controller → 前端更新`。修改方法签名、调用顺序、鉴权边界或异常分支时，必须同步更新相关注释、测试和项目流程文档。优先写清“调用谁以及为什么调用”，再写实现细节。

向项目维护者解释方法调用时，必须结合 Elara 用户实际使用项目的场景举例：例如打开网页、注册、登录、刷新页面、切换会话、发送消息、接收 AI 流式回复或退出登录。说明方法在用户操作后的哪一环被触发、由谁调用、为什么在这一环调用、下一步调用谁，以及返回结果会被哪一层继续使用。优先用项目真实调用链解释，再补充抽象框架概念，避免只给脱离用户流程的定义。

## Redis 引入规划

当前开发阶段暂不强制引入 Redis。满足以下任一条件时，应提醒项目维护者评估并引入 Redis：

- 后端部署为多个实例，需要跨实例共享会话锁或其他并发状态
- 登录 Session 访问频繁，或需要集中管理多设备登录状态
- AI 上下文缓存、短期记忆或摘要访问频率明显增加，PostgreSQL 查询成为性能瓶颈
- 需要接口限流、任务队列、实时状态或其他适合内存存储的临时数据

引入 Redis 时，长期用户资料、聊天记录、剧情进度、好感度和信任度仍以 PostgreSQL 为主要持久化存储。Redis 主要承担 Session、分布式锁、缓存和临时状态。AI 上下文相关代码应优先通过可替换的存储接口访问，避免将业务逻辑直接绑定到 Redis。

## 上线前用户系统安全清单

以下事项不阻塞当前功能开发，但在开始安全加固、公开部署或生产上线前，AI 必须主动提醒项目维护者逐项检查并完成：

- 增加 CSRF 防护，覆盖创建、修改、删除、退出登录等依赖 Cookie 的请求
- 生产环境必须配置强随机 `SESSION_SECRET`，不得使用开发默认值
- 增加登录限流和连续失败保护，防止暴力猜测密码；届时同时评估是否引入 Redis
- 增加过期 Session 的定时清理机制，避免 `sessions` 表长期累积无效记录
- 增加真实 Session E2E 测试，不得通过覆盖 Guard 来代替完整鉴权验证
- E2E 测试至少覆盖登录返回 Cookie、`/api/auth/me` 恢复身份、未登录返回 401、用户间数据隔离、退出后 Session 失效
- 使用 DTO 和统一验证机制完善邮箱、密码、显示名称及请求体字段校验
- 根据上线范围评估邮箱验证、修改密码、忘记密码、密码重置、注销账户和其他设备退出功能
- 生产环境启用 HTTPS，并确认 Cookie 的 `HttpOnly`、`Secure`、`SameSite`、有效期和清除参数配置一致

进入上述阶段时，应先检查本清单和现有测试，再制定安全加固顺序；完成后同步更新 README、测试和项目流程图。

## 开发约定

- 使用中文与项目维护者沟通和解释。
- 先阅读相关文件和现有测试，再做最小范围修改。
- 不提交 `.env`、真实 API Key、数据库密码或其他凭据。
- 不把 `DEV_USER_ID` 当作正式认证方案；它只是认证完成前的开发占位。
- 好感度、剧情状态、线索和结局应由后端状态逻辑控制，AI 主要负责角色表达。
- 修改后优先运行 `npm run build` 和相关测试。

## 常用命令

```bash
npm run start:dev
npm run build
npm test -- --runInBand
npm run test:e2e
npx prisma migrate status
npx prisma generate
```

## 环境变量

本机配置在 `.env`，公开模板在 `.env.example`。当前主要变量包括 `API_KEY`、`BASE_URL`、`MODEL`、`DATABASE_URL` 和开发阶段的 `DEV_USER_ID`。不要读取、输出或提交 `.env` 的真实内容。

## 继续工作前的建议

开始新任务时，先查看 `README.md` 的“当前进度”和“下一步计划”，再检查 Git 工作区状态。完成一个可验证阶段后，更新 README/本文件的进度并提交 Git。
